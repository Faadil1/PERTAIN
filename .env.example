# Set to 1 only for the clearly-labeled local seeded evaluation fallback.
PERTAIN_DEMO_MODE=1

# Gmail / Arga Gmail twin
GMAIL_BASE_URL=https://gmail.googleapis.com
GMAIL_ACCESS_TOKEN=
GMAIL_DRAFT_ID=

# Salesforce / Arga Salesforce twin
SALESFORCE_INSTANCE_URL=
SALESFORCE_ACCESS_TOKEN=
SALESFORCE_API_VERSION=v61.0

# Jira / Arga Jira twin
JIRA_BASE_URL=
JIRA_API_TOKEN=
JIRA_JQL=labels = pertain

# Optional bounded semantic model (OpenAI-compatible chat-completions API).
# Without these, PERTAIN uses a deterministic, explicitly-labelled parser fallback.
LLM_BASE_URL=
LLM_API_KEY=
LLM_MODEL=
